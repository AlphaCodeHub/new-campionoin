export default function StatusBar({ name, coins, level, onCreate, onSettings }) { return <header className="status-bar"><button className="profile-chip" onClick={onCreate} title="Create or replace character"><span className="profile-dot">{name[0]?.toUpperCase()}</span><span><small>COMPANION</small><strong>{name}</strong></span></button><div className="currencies"><span>◈ <b>{coins}</b></span><span>★ <b>LV. {level}</b></span></div><button className="settings-button" aria-label="Open settings" onClick={onSettings}>⚙</button></header>; }
