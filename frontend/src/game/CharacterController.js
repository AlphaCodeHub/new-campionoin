export const CharacterState = Object.freeze({ IDLE: 'IDLE', BLINK: 'BLINK', HAPPY: 'HAPPY', SAD: 'SAD', ANGRY: 'ANGRY', SURPRISED: 'SURPRISED', LAUGH: 'LAUGH', TALKING: 'TALKING', EATING: 'EATING', SLEEPING: 'SLEEPING', DANCING: 'DANCING', WAVE: 'WAVE', TAP_REACTION: 'TAP_REACTION' });
export class CharacterController { constructor(onStateChange = () => {}) { this.state = CharacterState.IDLE; this.onStateChange = onStateChange; } setState(nextState) { if (!Object.values(CharacterState).includes(nextState)) throw new Error(`Unknown state: ${nextState}`); this.state = nextState; this.onStateChange(nextState); } }
